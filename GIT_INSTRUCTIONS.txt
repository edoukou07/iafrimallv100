MANUEL - Étapes pour Pousser vers GitHub
═══════════════════════════════════════════════════════════════════════════════

⚠️ IMPORTANT: Exécutez les commandes depuis CMD ou Git Bash, PAS depuis PowerShell

═══════════════════════════════════════════════════════════════════════════════

ÉTAPE 1 : Ouvrir le Terminal (CMD ou Git Bash)
───────────────────────────────────────────────

Option A: CMD
- Appuyez sur: Windows + R
- Tapez: cmd
- Appuyez sur: Entrée

Option B: Git Bash
- Clic droit sur le dossier: c:\Users\edou\Desktop\IAAPP\image-search-api
- Sélectionnez: Git Bash Here

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 2 : Initialiser Git
──────────────────────────

Copier-coller dans le terminal:

cd c:\Users\edou\Desktop\IAAPP\image-search-api
git init

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 3 : Configurer Git (première fois uniquement)
──────────────────────────────────────────────────

Copier-coller:

git config --global user.name "Votre Nom"
git config --global user.email "votre.email@gmail.com"

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 4 : Ajouter tous les fichiers
───────────────────────────────────

Copier-coller:

git add .

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 5 : Créer le commit initial
──────────────────────────────────

Copier-coller:

git commit -m "Initial commit: Complete Image Search API with CLIP + Qdrant + Redis - FastAPI application with CLIP embeddings, Qdrant vector search, Redis caching, Docker setup, comprehensive documentation and tests"

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 6 : Ajouter la remote GitHub
──────────────────────────────────

Copier-coller:

git remote add origin https://github.com/edoukou07/iafrimallv100.git

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 7 : Renommer la branche en 'main'
───────────────────────────────────────

Copier-coller:

git branch -M main

───────────────────────────────────────────────────────────────────────────────

ÉTAPE 8 : Pousser vers GitHub
──────────────────────────────

Copier-coller:

git push -u origin main

⚠️  Vous devrez peut-être entrer vos identifiants GitHub:
    - Username: votre_username_github
    - Password: votre_token_personnel

Pour générer un token:
1. Aller sur: https://github.com/settings/tokens
2. Cliquer sur: Generate new token (classic)
3. Sélectionner les permissions (cochez "repo")
4. Générer et copier le token
5. Utiliser ce token comme mot de passe

───────────────────────────────────────────────────────────────────────────────

✅ VÉRIFICATION DU SUCCÈS
──────────────────────────

Après le push, vous devriez voir:

✓ Branch 'main' set up to track 'origin/main'.
✓ Everything up-to-date (ou des fichiers uploadés)

Allez vérifier sur GitHub:
https://github.com/edoukou07/iafrimallv100

Vous devriez voir tous les fichiers du projet!

───────────────────────────────────────────────────────────────────────────────

❓ EN CAS DE PROBLÈME
────────────────────

"fatal: not a git repository"
→ Vous n'êtes pas dans le bon dossier
→ Vérifiez: cd c:\Users\edou\Desktop\IAAPP\image-search-api

"Authentication failed"
→ Vérifiez votre username/password ou token GitHub

"fatal: refusing to merge unrelated histories"
→ Essayez: git push -u origin main --force

"remote already exists"
→ Essayez: git remote remove origin
→ Puis: git remote add origin https://github.com/edoukou07/iafrimallv100.git

───────────────────────────────────────────────────────────────────────────────

📋 TOUTES LES COMMANDES EN UNE SEULE FOIS
──────────────────────────────────────────

Si vous voulez copier-coller tout en bloc dans Git Bash:

cd c:\Users\edou\Desktop\IAAPP\image-search-api
git init
git config --global user.name "Votre Nom"
git config --global user.email "votre.email@gmail.com"
git add .
git commit -m "Initial commit: Complete Image Search API"
git remote add origin https://github.com/edoukou07/iafrimallv100.git
git branch -M main
git push -u origin main

───────────────────────────────────────────────────────────────────────────────

💡 CONSEILS
───────────

• Utilisez Git Bash plutôt que CMD pour moins de problèmes
• Assurez-vous d'être dans le bon dossier (image-search-api)
• Vérifiez votre connexion Internet
• Attendez 1-2 minutes après le push (GitHub doit indexer)

───────────────────────────────────────────────────────────────────────────────
